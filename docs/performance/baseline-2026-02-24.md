# Performance Baseline — 2026-02-24

## Environment

- **Machine**: macOS, Apple Silicon
- **Node**: v22.x
- **Next.js**: 16.x (App Router, Turbopack dev)
- **Lighthouse**: 12.6.1 (via @lhci/cli 0.15.1)
- **Preset**: Desktop (simulated throttling)
- **Runs per URL**: 3

## Lighthouse Scores (median)

| Page        | Performance | Accessibility | Best Practices | SEO |
| ----------- | ----------- | ------------- | -------------- | --- |
| `/` (Home)  | **100**     | **100**       | **100**        | 90  |
| `/login`    | **100**     | 96            | **100**        | 90  |
| `/contacts` | **100**     | **100**       | **100**        | 90  |

## Core Web Vitals (median, desktop)

| Metric | `/`    | `/login` | `/contacts` | Threshold (Good) |
| ------ | ------ | -------- | ----------- | ---------------- |
| FCP    | 254 ms | 249 ms   | 250 ms      | < 1800 ms        |
| LCP    | 558 ms | 547 ms   | 549 ms      | < 2500 ms        |
| TBT    | 0 ms   | 0 ms     | 0 ms        | < 200 ms         |
| CLS    | 0      | 0        | 0           | < 0.1            |
| SI     | —      | —        | —           | < 3400 ms        |

All Core Web Vitals in the **"Good"** range.

## Notes

- `/login` has A11y 96 due to missing `autocomplete` attribute on password field — scheduled for Phase 3 (UI/UX improvements)
- SEO 90 across all pages — missing `<meta name="description">` on some pages (resolved by adding per-page metadata)
- `/todos` not tested because it requires authentication (Lighthouse runs unauthenticated)
- No artificial delays in data fetching functions

## Bundle Analysis

Run `pnpm analyze` to generate interactive bundle treemap at `.next/analyze/`.

## How to Reproduce

```bash
pnpm build
pnpm lighthouse        # runs full audit (collect + assert + upload)
pnpm lighthouse:collect # collect only, skip assertions
```
