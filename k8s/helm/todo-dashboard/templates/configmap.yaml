apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-dashboard.fullname" . }}-config
  labels:
    {{- include "todo-dashboard.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
